
@model IEnumerable<Modulo1_Administracion.Models.mesas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cantidad_personas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombre_mesa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cantidad_personas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_mesa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_mesa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_mesa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_mesa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
